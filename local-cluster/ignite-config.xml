<?xml version="1.0" encoding="UTF-8"?>
<!--
  Single config shared by all three localhost Ignite nodes.

  Because thin-client and discovery ports auto-increment when already in use,
  starting three nodes sequentially with this one config yields:
    node 1 → thin 10800, discovery 47500
    node 2 → thin 10801, discovery 47501
    node 3 → thin 10802, discovery 47502
  The static VM IP finder lists the whole localhost discovery range so the
  nodes form a single cluster.
-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">
    <bean class="org.apache.ignite.configuration.IgniteConfiguration">

        <!-- Thin-client connector; auto-increments from 10800 per node. -->
        <property name="clientConnectorConfiguration">
            <bean class="org.apache.ignite.configuration.ClientConnectorConfiguration">
                <property name="port" value="10800"/>
                <property name="portRange" value="10"/>
            </bean>
        </property>

        <!-- A partitioned cache with one backup. -->
        <property name="cacheConfiguration">
            <list>
                <bean class="org.apache.ignite.configuration.CacheConfiguration">
                    <property name="name" value="demo"/>
                    <property name="cacheMode" value="PARTITIONED"/>
                    <property name="backups" value="1"/>
                    <property name="atomicityMode" value="ATOMIC"/>
                </bean>
            </list>
        </property>

        <!-- Static localhost discovery; ports auto-increment from 47500. -->
        <property name="discoverySpi">
            <bean class="org.apache.ignite.spi.discovery.tcp.TcpDiscoverySpi">
                <property name="localPort" value="47500"/>
                <property name="localPortRange" value="10"/>
                <property name="ipFinder">
                    <bean class="org.apache.ignite.spi.discovery.tcp.ipfinder.vm.TcpDiscoveryVmIpFinder">
                        <property name="addresses">
                            <list>
                                <value>127.0.0.1:47500..47509</value>
                            </list>
                        </property>
                    </bean>
                </property>
            </bean>
        </property>
    </bean>
</beans>
